<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.emn.trustydrive.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <Button
        android:text="Access documents"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonAddAccount"
        android:layout_above="@+id/buttonDocuments"
        android:layout_centerHorizontal="true"
        android:onClick="accessDocuments" />

    <Button
        android:text="Manage registered accounts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonManageAccounts"
        android:layout_below="@+id/buttonDocuments"
        android:layout_centerHorizontal="true"
        android:onClick="manageAccounts"/>

    <Button
        android:text="Add cloud accounts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonDocuments"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="addCloudAccounts"/>

    <Button
        android:text="Display Name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonDisplayName"
        android:onClick="displayName"
        android:layout_above="@+id/buttonAddAccount"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="Welcome to TrustyDrive!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/welcomeText"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        android:layout_above="@+id/buttonDisplayName"
        android:layout_alignEnd="@+id/buttonManageAccounts"
        android:layout_marginBottom="20dp" />

</RelativeLayout>
